package com.smarthome.application.enums;

public enum HomeTheme {
    ROMANTIC,
    CHILL,
    STRESS_RELIEF,
    PARTY,
    SOLITUDE,
    CALL_HELP,
    CUSTOM // for future use - set by the person home - no data should overwrite it without prompt - only direct device change inputs
}
